package AccessModifiers;

public class MemberLevel {

    /*public: at the memnber level, public has the same meaning as at top level. a public class member (or field) and public method can be accessed from any other class anywhere,
    even in a different package*/

    /*private: object in only visible within the class it is declared. it is not visible anywhere else (including in subclasses of its class)
     */

    /*protected: object is visible anywhere in its own package but also in subclasses even if they are in another package*/
}
