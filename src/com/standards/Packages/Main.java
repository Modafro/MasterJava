package com.standards.Packages; //naming standard for a package (should a be at project level) is to have com.something

public class Main {
    public static void main(String[] args) {
        MyWindow myWindow = new MyWindow("Complete Java");
        myWindow.setVisible(true);
    }
}
